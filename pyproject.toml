[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eversmart"
version = "0.1.0"
description = "Authenticated Eversource smart-meter retrieval and dashboard"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
